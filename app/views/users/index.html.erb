<% title "User list" %>
<h1>All Users</h1>
 <% @users.each do |user| %>
     <%= image_tag(user.avatar.url, height: '80', width: '80') %>
     <%= link_to user.name, controller: "users", action: "show", id: user %>
     <br>
 <% end %>